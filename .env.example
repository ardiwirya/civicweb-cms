# Ambil dari Supabase Dashboard > Project Settings > API
PUBLIC_SUPABASE_URL=
PUBLIC_SUPABASE_ANON_KEY=

# Dipakai astro.config.mjs untuk sitemap & canonical URL
PUBLIC_SITE_URL=

# Hanya dipakai saat generate types (npm run db:types), tidak dipakai runtime
SUPABASE_PROJECT_ID=
